{
  "//": "MIRRORS the gate flags in verification/run-gates.js. Gate S2 asserts they match - this file exists so your editor reports the same diagnostics the gates do, and drift between the two is caught rather than discovered.",
  "compilerOptions": {
    "strict": true,
    "noEmitOnError": true,
    "target": "ES2020",
    "module": "commonjs",
    "noEmit": true,
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true,
    "types": ["node"]
  },
  "include": ["*.ts"],
  "exclude": ["node_modules", ".gate-tmp", "build", "dist", "verification"]
}
