{
  "name": "starforge-stage0",
  "version": "0.0.0",
  "private": true,
  "description": "StarForge stage-0 source and conformance gates. The TypeScript version is pinned EXACTLY, not by range: the gates are a provenance artefact and their toolchain is part of the result (audit correction C11).",
  "scripts": {
    "gates": "node verification/run-gates.js",
    "status": "node verification/module-status.js",
    "status:write": "node verification/module-status.js --write",
    "typecheck": "tsc -p tsconfig.json",
    "pretest": "npm run gates",
    "test": "node -e \"console.log('the gates are the test suite')\""
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^6.0.3"
  }
}
